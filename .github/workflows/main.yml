on: push
jobs:
  versions:
    runs-on: windows-latest
    steps:
      - run: node --version
      - run: npm --version
  first-job:
    runs-on: windows-latest
    steps:
      - run: npm run fix-lint
  second-job:
    runs-on: windows-latest
    steps:
      - run: npm run start-server
