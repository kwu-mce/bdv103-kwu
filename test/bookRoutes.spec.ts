import Router from 'koa-zod-router';
import { testSetup, testTerminate } from "./helper";

const router = Router();

// create test routes?
// use testSetup() to initialize connection and test data?
// execute test route and evaluate?
// use testTerminate() to reset data and connection  ?

export default router;