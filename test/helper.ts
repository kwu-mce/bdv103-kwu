import { beforeEach, afterEach } from "node:test";

export function testSetup() {

    beforeEach(() => {

        //connect to db
        //specify test book schema
        //return connection? 
    
    });
};


export function testTerminate() {
    afterEach(() => {

        //remove db objects
        //terminate connect
        
    });
}